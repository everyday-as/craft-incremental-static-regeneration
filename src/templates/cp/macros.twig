{% macro configWarning(setting) -%}
    {% set setting = '<code>'~setting~'</code>' %}
    {{ 'This is being overridden by the {setting} config setting in your {file} config file.'|t('incremental-static-regeneration', {
        setting: setting,
        file: 'incremental-static-regeneration.php',
    })|raw }}
{%- endmacro %}